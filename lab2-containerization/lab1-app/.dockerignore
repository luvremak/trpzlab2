# Keep the build context small and the image clean.
__pycache__/
*.py[cod]
.venv/
venv/
*.log

# These are mounted at runtime, not baked into the app image.
docker-compose.yml
nginx-mywebapp.conf
config.toml
README.md
